---
title: SAM PMC
id: atmel,sam-pmc
version: 0.1

description: >
    This binding gives a base representation of the SAM Power Management Controller for Clock control

properties:
  - compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "atmel,sam-pmc"

  - reg:
      type: array
      description: mmio register space
      generation: define
      category: required

cell_string: clocks

"#cells":
  - bus
  - bits

...
