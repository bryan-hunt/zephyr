---
title: Atmel SAM Pinmux
id: atmel,sam-pinmux
version: 0.1

description: >
    This is a representation of the SAM Pinmux node

properties:
  - compatible:
      type: string
      category: required
      description: compatible strings
      constraint: "atmel,sam-pinmux"

  - reg:
      type: int
      description: mmio register space
      generation: define
      category: required

  - label:
      type: string
      category: required
      description: Human readable string describing the device (used by Zephyr for API name)
      generation: define

"#cells":
  - pin
  - function
...
